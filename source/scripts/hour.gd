extends Control

var hour : int

@onready var first_half = $"half_hour_container/half_hour"
@onready var second_half = $"half_hour_container/half_hour2"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass
